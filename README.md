# Rick & Morty Characters – React + Vite

This project is a small React application that displays characters from the Rick & Morty API.  
It includes search, filtering, debouncing, global caching of characters, and a clean card UI.

---

## 🚀 Features Implemented

### ✅ Core Requirements>>>>>> 

- Fetches **all characters** once and stores them in state.
- Displays character list with images, name, species, gender, and origin.
- **Search bar** with input debouncing.
- **Filter options**:
  - Status (Alive / Dead / Unknown)
  - Gender (Male / Female / Genderless / Unknown)
- Fully responsive card layout.
- “**No characters found**” fallback message.
- Project structured into reusable components.

### 🎁 Bonus Features >>>>>>>>
- Global fetch caching to avoid repeated API calls.
- Search + filter combination logic.
- Skeleton loading cards.
- Keyboard-friendly search.
- React Testing Library added with unit tests.

---

 ###  How to Run the Project >>>>>>>>
1. Clone the repository
git clone https://github.com/YOUR_USERNAME/XYZ.git
cd XYZ

2. Install dependencies
npm install

3. Start development server
npm run dev
